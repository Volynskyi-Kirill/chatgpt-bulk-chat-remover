{
  "name": "chatgpt-bulk-chat-remover",
  "version": "2.0.0",
  "description": "Chrome extension for bulk deleting ChatGPT chats",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "dev": "npm run clean && npm run watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.0"
  },
  "private": true
}
